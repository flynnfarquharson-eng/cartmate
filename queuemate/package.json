{
  "name": "queuemate",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that alerts you when your ticket queue is nearly done",
  "scripts": {
    "test": "node --test test/*.test.js",
    "icons": "node scripts/make-icons.js",
    "zip": "zip -r queuemate.zip manifest.json src icons"
  }
}
